[package]
name = "arb-engine"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
tick-math    = { path = "../tick-math" }
curve-engine = { path = "../curve-engine" }
oms-core     = { path = "../oms-core" }
